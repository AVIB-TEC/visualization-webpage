apiVersion: v1
kind: Service
metadata:
    name: "avibcluster-47cf"
    labels:
        app: "avibcluster-47cf"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "avibcluster-47cf"